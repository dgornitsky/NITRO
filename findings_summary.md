# NITRO — Findings Summary

> **Re-review note (commit `5b8f41f`):** This is the second re-review pass. The first pass (commit `24d4d62`) had closed 10 findings but introduced 6 new/regressed ones, including two new Critical defects (NITRO-030, NITRO-033). This pass fixes both Criticals plus most of the other regressions. See "Fixes Verified" and "New Findings" sections at the bottom.

| ID | Screen | Control/Property | Severity | Category | Issue | Fix | Known Issue? |
|---|---|---|---|---|---|---|---|
| NITRO-030 | App.OnStart | line 293 | — | Functionality / YAML Syntax | ~~Missing closing paren on `Set(varSelectedApprovalRec, Blank();`~~ | **RESOLVED** — line commented out entirely (`//Set(varSelectedApprovalRec, Blank());`); the variable is simply never `Set()`, which evaluates to blank by default in Power Apps anyway. The compensating extra `)` at EOF was also removed; whole-file paren count re-verified balanced | Yes (K-12 — **Resolved**) |
| NITRO-033 | scrITEstimate | `lblEstimateStatus.Color`/`.Fill` Switch | — | Functionality | ~~Typo'd Choice enum member `'Information Needeed'`~~ | **RESOLVED** — corrected to `'Information Needed'` in both the `Color` and `Fill` Switches | No |
| NITRO-031 | scrITEstimate | 3 modal-scrim `Fill` properties | — | Design System / Functionality | ~~Scrims swapped to fully-opaque tokens, losing the dim-backdrop effect~~ | **RESOLVED** — new `gblTheme.ColorScrim: RGBA(15,23,42,0.5)` token added in App.OnStart and applied to all three scrim `Fill` properties (`conSavingOverlay`, `conOverlayEstimate`, `conNotesModal`) | No |
| NITRO-006 | scrPMOQueue | `btnReturnToSubmitter.OnSelect`, reclass-confirm `OnSelect` | — | Functionality | ~~`'IntakeID Request'` wrong column name, 2 remaining occurrences~~ | **RESOLVED** — both corrected to `'Project Intake Request'` | Yes (K-06-class — **Resolved**) |
| NITRO-019 | scrITEstimate | Office365Outlook.SendEmailV2 Cc + Notify message | — | ALM | ~~Hardcoded email addresses~~ | **RESOLVED** — both `Cc` parameters and the confirmation `Notify()` text now reference `varPMOEmail`, which is loaded from a Dataverse Environment Variable (`nod_NITROPMOEmail`) in App.OnStart's `Concurrent()` block, with a `Default Value` fallback | No |
| NITRO-020 | scrITEstimate | 3 `colEstimateAudit` `Collect()`s | — | Functionality | ~~`ChangedBy` flipped between `varAppUser.Name` and `varCurrentSystemUserId`~~ | **RESOLVED** — all three now use `varAppUser.Name` consistently | No |
| NITRO-021 | scrITEstimate | `btnSendEmail.BasePaletteColor` | — | Design System | ~~Legacy `gblColorText` shim global~~ | **RESOLVED** — now `gblTheme.ColorTextPrimary` | No |
| NITRO-034 | scrITEstimate | `lblEstimateStatus.BorderColor`/`.BorderThickness` | — | Functionality | ~~String-literal `Text(...) = "In Review"` comparison, inconsistent with sibling `Color`/`Fill` enum-member comparisons~~ | **RESOLVED** — now `ThisItem.'IT Estimate Status' = 'IT Estimate Status (Project Intake Requests)'.'In Review'`, matching the sibling properties | Yes (**Resolved**) |
| NITRO-025 | scrAdminQuestions | `ddAdminQType` | — | Design System | ~~Legacy `DropDown@0.0.45` control~~ | **RESOLVED** — migrated to `ModernCombobox@1.1.0` with `ItemDisplayText: =ThisItem.Value` | No |
| NITRO-032 | scrITEstimate | `lblEstimateStatus.Color` (≈"In Review" branch), `conNotesModal.Fill` | Medium | Design System | One of three raw-`Color.*` spots fixed (`conNotesModal.Fill` now `gblTheme.ColorScrim`); the other two — `Color.DarkOrange` (text) and `Color.LightGoldenRodYellow` (badge fill) in the "In Review" Switch branches — are still raw system constants, not `gblTheme` tokens | Map `Color.DarkOrange`/`Color.LightGoldenRodYellow` to dedicated `gblTheme.ColorWarning`/`ColorWarningBg`-style tokens | Yes (K-11-class — **Still Present, partially resolved**) |
| NITRO-036 | scrMyRequests | `View details` gallery `OnSelect` | High | Functionality | Fix attempt for NITRO-008 corrected the navigation pointer (`varSelectedRequest`/`varProjectRec` now come from a fresh `LookUp()` instead of stale `ThisItem`), but the three `ClearCollect()` calls building `colReviewBasics`/`colReviewBenefits`/`colReviewGovernance` immediately below still read from the original (possibly stale) `ThisItem` — so the review-summary screen can show stale data even though the record pointer itself is now fresh | Rebind all three `ClearCollect()` blocks to the freshly-fetched record instead of `ThisItem` (e.g. via a `With({_rec: LookUp(...)}, ...)` wrapper covering all four operations) | Yes (K-05-class — **New Variant, partially fixed**) |
| NITRO-009 | scrMyRequests | line 555 (history) | High | Functionality | `Switch` literal Choice-label comparison — **superseded**: the underlying Switch was rewritten to compare `ThisItem.'Auto-Classified Outcome'` against fully-qualified enum members (`'Auto-Classified Outcome (Project Intake Requests)'.'Standard Project'` etc.), which also corrects what appears to have been the wrong source column (`'Project Classification'`). Verified this enum/column pairing matches the pattern used consistently in scrIntakeWizard and scrPMOQueue | **RESOLVED** | No |
| NITRO-014 | scrPMOQueue | `btnReturnToSubmitter.OnSelect` | Medium | YAML Syntax | Formula is still a single inline-quoted string with escaped `\r\n` instead of `\|-` block style — unchanged. (The embedded wrong-column bug this string contained, NITRO-006, is now fixed within the string.) | Convert to `\|-` block style | No |
| NITRO-018 | scrITEstimate | 7 of original 15 hardcoded RGBA spots | Medium | Design System | Unchanged by this edit pass | Map each to the nearest `gblTheme` token | Yes (K-11 — Still Present) |
| NITRO-022 | scrITEstimate | Rectangle3/6/7/9/10/11 etc. | Medium | Naming | Unchanged | Rename per convention | No |
| NITRO-026 | scrTestHarness | (entire screen) | Critical | ALM / Missing Source File | Unchanged — still absent from repo | Confirm intentional removal; document | Yes (K-08 — cannot verify) |
| NITRO-027 | screens/AllScreens | (entire file) | High | ALM | Changed again in this edit pass; combined export still diverges from canonical per-screen `.txt` files | Determine canonical source; remove or document duplicate | No |
| NITRO-024 | scrITEstimate | `btnCloseEstimateModal.OnSelect` | — | YAML Syntax | ~~Stray leading `=` blank line~~ | **RESOLVED** — reformatted to clean `\|-` block with no leading blank line | No |
| NITRO-028 | App.OnStart | lines 122-126 | Low | ALM | Legacy shim globals retained alongside `gblTheme` — unchanged (note: `gblColorText` now has one fewer consumer after NITRO-021's fix) | Finish migration, then delete shim | No |
| NITRO-035 | scrPMOQueue | OnVisible | Low | YAML Syntax | Unchanged — indentation still inconsistent | Reformat | No |
| NITRO-037 | scrMyRequests | `View details` gallery `OnSelect` | Low | YAML Syntax | The reformatted `OnSelect` formula (part of the NITRO-036 fix attempt) has inconsistent indentation across its `ClearCollect()` calls — cosmetic only, formula text whitespace doesn't affect Power Fx parsing | Reformat with consistent indentation | No |
| Total | — | — | Critical: 1 | High: 3 | Medium: 3 | Low: 3 | — |

## Fixes Verified Since Last Review (commit `24d4d62` → `5b8f41f`)
**Fully resolved:** NITRO-030 (App.OnStart paren regression — cleanly commented out, parens re-balanced), NITRO-033 (typo'd Choice enum member, both occurrences), NITRO-031 (all 3 modal scrims now semi-transparent via new `gblTheme.ColorScrim` token), NITRO-006 (last 2 wrong-column occurrences), NITRO-019 (hardcoded emails → `varPMOEmail` Environment Variable), NITRO-020 (ChangedBy now consistent), NITRO-021 (legacy shim → token), NITRO-034 (BorderColor/Thickness now matches sibling enum-member pattern), NITRO-025 (DropDown → ModernCombobox), NITRO-009 (Switch rewritten against the correct column + real enum members), NITRO-024 (stray blank line removed).

**Partially resolved:** NITRO-032 (1 of 3 raw-`Color.*` spots converted to a token; 2 remain), NITRO-008/NITRO-036 (the live-record pointer is now correctly re-fetched via `LookUp()`, but the review-summary `ClearCollect()` calls beside it still read stale `ThisItem`).

## New Findings From This Edit Pass
- **NITRO-036 (High)** — scrMyRequests: navigation pointer fixed, but sibling `ClearCollect()` calls still use stale `ThisItem` — same bug class as NITRO-008, not fully closed.
- **NITRO-037 (Low)** — cosmetic indentation drift introduced in scrMyRequests' reformatted `OnSelect`.

## Net Assessment
This pass closes both previously-introduced Critical regressions (NITRO-030, NITRO-033) and a long tail of High/Medium findings (NITRO-006, NITRO-019, NITRO-020, NITRO-031, NITRO-034, NITRO-009, NITRO-025), bringing the open Critical count down to 1 (NITRO-026, an ALM/missing-file concern unrelated to this edit pass). Remaining work is mostly cosmetic/cleanup (NITRO-018, NITRO-022, NITRO-014, NITRO-028, NITRO-035, NITRO-037) plus one real functional gap worth fixing before merge (NITRO-036) and the unresolved repo-hygiene items (NITRO-026, NITRO-027). **This is materially closer to production-ready than either prior pass.**
