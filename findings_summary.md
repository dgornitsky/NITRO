# NITRO — Findings Summary

| ID | Screen | Control / Property | Severity | Category | Issue | Fix | Known Issue? |
|---|---|---|---|---|---|---|---|
| NITRO-001 | App.OnStart | `varIsAdmin`/`varIsPMO`/`varIsITEstimateManager`/`varIsApprover` (lines 207-210) | Critical | Functionality | Role flags use `If(varAppUser.X = true, true, false)` instead of the bare null-safe `varAppUser.X = true` pattern | Replace each `If(..., true, false)` with the bare boolean expression `varAppUser.X = true` | Yes (K-02 — New Variant; not literally `Coalesce()` but the equally-banned `If(...,true,false)` wrapper) |
| NITRO-002 | scrIntakeWizard | OnVisible, `Concurrent()` reset block | Critical | Functionality | The reset block runs unconditionally on every `OnVisible`, even in Resume mode, overwriting the Resume-mode state just set by the preceding `If()` | Gate the `Concurrent()` reset block inside `If(varWizardMode = "New", Concurrent(...))` | No |
| NITRO-003 | scrIntakeWizard | line 1587 | Critical | Functionality / Naming | `If(CategoryStepValid, DisplayMode.Edit Or IsBlank(txtBenefitAmount.Text), DisplayMode.Disabled)` references an undefined identifier (missing `var` prefix) and mixes an enum value with a boolean `Or` | Use `varCategoryStepValid`; rewrite as `If(varCategoryStepValid Or IsBlank(txtBenefitAmount.Text), DisplayMode.Edit, DisplayMode.Disabled)` | No |
| NITRO-005 | scrPMOQueue | line 435, `btnSubmitToIT.DisplayMode` | Critical | Functionality / Naming | Undefined identifier `PMOStage` (missing `var` prefix) | Rename to `varPMOStage` | No |
| NITRO-006 | scrPMOQueue | lines 464, 624 | Critical | Functionality | `RemoveIf(..., 'IntakeID Request' = _patched.'IntakeID Request')` references a column name that does not exist on `'Project Intake Requests'`; the real column (confirmed in scrIntakeWizard/scrReview) is `'Project Intake Request'` | Change both to `'Project Intake Request' = _patched.'Project Intake Request'` | No |
| NITRO-007 | scrITEstimate | line 227 | Critical | Functionality | Same `'IntakeID Request'` vs `'Project Intake Request'` column-name mismatch in `RemoveIf(colEstimateQueue, ...)` | Change to `'Project Intake Request' = varProjectRec.'Project Intake Request'` | No |
| NITRO-017 | scrITEstimate | `galEstimateRequests.OnSelect` (lines 337-339) | Critical | Functionality | Sets `varProjectRec`/`varSelectedIntakeRequest`/`varITEstimate` directly from `ThisItem` instead of a live `LookUp()` by GUID | Re-fetch via `LookUp('Project Intake Requests', nod_projectintakerequestid = ThisItem.nod_projectintakerequestid)` before setting these vars | Yes (K-03 — New Variant; the originally-described "LookUp inside Patch" defect is gone, but ThisItem hydration is now the live bug here) |
| NITRO-026 | scrTestHarness | (entire screen) | Critical | ALM / Missing Source File | `scrTestHarness.txt` is in scope but absent from the repository entirely (only App.OnStart + 7 named screens exist under `/screens`) | Confirm intentional removal before Prod export and document it; if it was meant to still exist in Dev for UAT, restore it to source control | Yes (K-08 — cannot verify; file is absent, so presence-in-Prod cannot be confirmed either way) |
| NITRO-029 | App.OnStart | line 291 | Critical | Functionality | `Set(varSelectedApprovalRec, "");` initializes a record-typed variable with `""` instead of `Blank()`, which locks its type as text and will break later record assignment | Change to `Set(varSelectedApprovalRec, Blank());` | Yes (K-12 — Still Present) |
| NITRO-027 | screens/AllScreens | (entire file, 7648 lines) | High | ALM | An undocumented combined export file with no extension exists alongside the per-screen `.txt` files and contains materially different/healed code for several reviewed defects (e.g. `varCategoryStepValid` correctly named at its line 3098, and `varSelectedApprovalRec` shown as `Blank()` in a comment at line 307) — the tracked per-screen exports may be stale relative to the actual app source | Determine canonical source; remove the stale duplicate or document it as an excluded generated artifact | No |
| NITRO-010 | scrHome | line 80, `drpQuickRole` | High | Role Access | A role-switcher dropdown (Admin/PMO/IT Estimate Manager/Approver/Unauthorized) is present directly on the production Home screen | Remove from production build, or gate `Visible` behind a non-production environment flag | No |
| NITRO-011 | scrHome | line 371, `btnRecentOverlay.OnSelect` | High | Functionality | `Navigate(scrReview, ScreenTransition.Fade)` fires without first setting `varProjectRec`/`varSelectedRecord` from `ThisItem`, so scrReview opens against stale/wrong context | Set the record variable (ideally via live `LookUp()` on GUID) before navigating | No |
| NITRO-008 | scrMyRequests | lines 434-435 | High | Performance / Functionality | Gallery row actions capture `ThisItem` directly instead of re-fetching the live record by GUID via `LookUp()` | Re-`LookUp()` the live record by GUID before acting, per the pattern in scrPMOQueue's `galQueue.OnSelect` | Yes (K-05 — New Variant; resolved on scrPMOQueue itself, still present here) |
| NITRO-009 | scrMyRequests | line 555 | High | Functionality | `Switch` branches on the literal `"Steering Committee Project"` against a Choice column; this string does not appear to match the actual enum member label used elsewhere | Replace with the fully-qualified Choice/enum reference, or `Text()`-wrap the comparison against the canonical label | No |
| NITRO-013 | scrPMOQueue | lines 659, 670 | High | ALM | `cmpPrimaryNav.EnvironmentLabel` and `CurrentScreen` left as placeholder literal `"Text"` | Set `EnvironmentLabel` to the real environment-label expression and `CurrentScreen` to `"queue"` | No |
| NITRO-015 | scrPMOQueue | OnVisible | High | Role Access | Unlike scrITEstimate/scrAdminQuestions, OnVisible never sets `varRedirectToHome` based on a role check, even though `tmrRedirectGuard` (`Start: =varRedirectToHome`) is wired and ready | Add the same `If(Not(varIsAdmin Or varIsPMO), Set(varRedirectToHome, true), Set(varRedirectToHome, false); ...)` gate used on scrAdminQuestions/scrITEstimate | No |
| NITRO-019 | scrITEstimate | lines 208, 1333, 1354 | High | ALM | Hardcoded email addresses `dgornitsky@nationaloak.com` and `pmo@nationaloak.com` used as CC targets in `Office365Outlook.SendEmailV2` | Move to Environment Variables so emails aren't baked into managed solution layers | No |
| NITRO-004 | scrIntakeWizard | line 1662 | Medium | Naming | Bare `CategoryStepValid;` statement (missing `var` prefix) used as a no-op formula line | Remove, or correct to `varCategoryStepValid;` | No |
| NITRO-012 | scrPMOQueue | line 671 | Medium | Design System | Hardcoded `RGBA(1, 81, 52, 1)` on `cmpPrimaryNav_5.Fill` instead of a `gblTheme` token | Replace with the matching `gblTheme.ColorBrandPrimary`/equivalent token | Yes (K-11 — Still Present) |
| NITRO-014 | scrPMOQueue | `btnReturnToSubmitter.OnSelect` | Medium | YAML Syntax | Multi-line formula written as an inline-quoted string instead of the `\|-` block scalar | Convert to `\|-` block style | No |
| NITRO-018 | scrITEstimate | lines 383, 401, 414, 463, 465, 1032, 1119, 1198, 1225, 1232, 1264, 1568, 2020, 2093, 2116 | Medium | Design System | 15 hardcoded `RGBA(...)` literals instead of `gblTheme` tokens — the highest concentration of any screen | Map each to the nearest `gblTheme` token, adding new tokens if genuinely new colors are required | Yes (K-11 — Still Present) |
| NITRO-020 | scrITEstimate | line 218 vs lines 1299, 1347 | Medium | Functionality | `ChangedBy` is `varAppUser.Name` in one audit `Collect()` but `varCurrentSystemUserId` in two others on the same screen | Standardize on one identity representation across all audit Collects | No |
| NITRO-022 | scrITEstimate | lines 290, 587, 647, 707, 766, 902, 937 | Medium | Naming | Default control names retained: `Rectangle3`, `Rectangle6`, `Rectangle7`, `Rectangle7_2`, `Rectangle9`, `Rectangle10`, `Rectangle11`, `DataGrid2` | Rename per the `rec*`/`gal*`/`tbl*` convention | No |
| NITRO-023 | scrITEstimate | status-label `Switch`/`If` literals | Medium | Functionality (unverified) | String literals such as `"Submitted to IT"`/`"IT Estimate Complete"` compared against `Text('IT Estimate Status')` may not exactly match the real Choice option labels (`.Received`, `.'In Review'`, `.Complete`) | Verify each literal against the Choice column's actual option-set text; replace with fully-qualified enum + `Text()` wrap | No |
| NITRO-025 | scrAdminQuestions | throughout | Medium | Design System | Screen exclusively uses legacy non-`Modern*` controls (`Button@0.0.45`, `DropDown@0.0.45`, `Toggle@1.1.5`, `Rectangle@2.3.0`) while other screens have migrated to the Modern control set | Migrate to `ModernButton`/`ModernDropdown`/etc. for consistency | No |
| NITRO-021 | scrITEstimate | line 1315 | Low | Design System | `btnSendEmail.BasePaletteColor: =gblColorText` uses the legacy shim global instead of `gblTheme.ColorTextPrimary` | Replace with `gblTheme.ColorTextPrimary` | No |
| NITRO-024 | scrITEstimate | `btnCloseEstimateModal.OnSelect` | Low | YAML Syntax | Formula begins with a stray `=` on its own line before the first statement | Remove the leading blank line | No |
| NITRO-028 | App.OnStart | lines 120-125 | Low | ALM | Legacy shim globals (`gblColorGreen`, `gblColorRed`, `gblColorMuted`, `gblColorText`, `gblColorWhite`) retained alongside `gblTheme`, and still referenced from scrITEstimate (NITRO-021) | Finish migrating off the shim, then delete it | No |
| Total | — | — | Critical: 9 | High: 7 | Medium: 9 | Low: 3 | — |

**Note on Known Issues not independently re-creatable from YAML alone:** K-09 (unmanaged connection-reference dependency, Solution Checker) is a solution-package-level concern, not visible in screen YAML — carried forward as **Still Present** per the stated known blocker, with no contradicting evidence found. K-06 (Navigate() in OnVisible) and K-07 (direct Patch to 'Project Intake Audit') were checked against every screen read and **not found anywhere** — both are **Resolved** app-wide. K-04 (`btnApproveRoute.OnSelect` missing `IfError()`) and K-05 (`galQueue.OnSelect` using `ThisItem`) are both **Resolved** specifically on scrPMOQueue. K-01 (`varCurrentUser` legacy block) — **Resolved**; no occurrence of `varCurrentUser` found anywhere in the repository. K-10 (missing `Concurrent()`) is **Resolved** on scrPMOQueue (used correctly for 3 independent queries).
