# NITRO — Findings Summary

> **Re-review note:** This table reflects the repository state as of commit `24d4d62` (after the latest GitHub edits). A separate "Fixes Verified" and "New Regressions Introduced" account appears at the bottom — several fix attempts only partially resolved their target finding, and one introduced a new Critical syntax defect.

| ID | Screen | Control/Property | Severity | Category | Issue | Fix | Known Issue? |
|---|---|---|---|---|---|---|---|
| NITRO-030 | App.OnStart | line 291, `varSelectedApprovalRec` | Critical | Functionality / YAML Syntax | Fix attempt for K-12 left a dangling unclosed paren: `Set(varSelectedApprovalRec, Blank();` (missing `)` before the `;`). Because the *last* statement in the file (`ClearCollect(colCmpDetailsListColumns, ...)`) was given a *compensating extra* `)`, the file's overall paren count is balanced — but everything between line 291 and the end of the file is now nested as a single chained argument inside the outer `Set(`, not separate top-level statements. `varSelectedApprovalRec` ends up bound to the return value of the final `ClearCollect`, not `Blank()`. | Rewrite as two independently-closed statements: `Set(varSelectedApprovalRec, Blank());` followed by the next `Set(...)` on its own line | Yes (K-12 — fix attempt failed; **New Variant**, worse than the original bug) |
| NITRO-033 | scrITEstimate | `lblEstimateStatus.Color`/`.Fill` Switch (≈lines 399, 415) | Critical | Functionality | `Switch()` now references `'IT Estimate Status (Project Intake Requests)'.'Information Needeed'` — appears to be a typo of "Information Needed". If this string doesn't exactly match a real Choice option label, this is an undefined-member reference that will fail to compile/save in Power Apps Studio. | Correct the enum member name to match the actual Choice option label exactly (verify casing/spelling in Dataverse) | No |
| NITRO-001 | App.OnStart | `varIsAdmin`/`varIsPMO`/`varIsITEstimateManager`/`varIsApprover` | — | Functionality | ~~Role flags used `If(varAppUser.X = true, true, false)`~~ | **RESOLVED** — now `Set(varIsAdmin, varAppUser.IsAdmin = true)` etc., bare boolean pattern | Yes (K-02 — **Resolved**) |
| NITRO-002 | scrIntakeWizard | OnVisible | — | Functionality | ~~Reset `Concurrent()` block ran unconditionally, overwriting Resume-mode state~~ | **RESOLVED** — `Concurrent()` reset is now correctly nested as the `If()`'s false-branch (`If(Resume-condition, ...resume statements..., Concurrent(...reset...))`), confirmed paren-balanced | No |
| NITRO-003 | scrIntakeWizard | line 1584 | — | Functionality / Naming | ~~Undefined `CategoryStepValid` identifier~~ | **RESOLVED** — corrected to `varCategoryStepValid` | No |
| NITRO-004 | scrIntakeWizard | line 1659 | — | Naming | ~~Bare `CategoryStepValid;` statement~~ | **RESOLVED** — corrected to `varCategoryStepValid;` | No |
| NITRO-005 | scrPMOQueue | `btnSubmitToIT.DisplayMode` | — | Functionality / Naming | ~~Undefined `PMOStage` identifier~~ | **RESOLVED** — corrected to `varPMOStage` | No |
| NITRO-006 | scrPMOQueue | line 628, reclass-confirm `OnSelect` | High | Functionality | `RemoveIf(colPMOTriageQueue, 'IntakeID Request' = _patched.'IntakeID Request')` — wrong column name still present at this second occurrence (the `btnSubmitToIT` occurrence was fixed; this one in the reclassification-confirm flow was not) | Change to `'Project Intake Request' = _patched.'Project Intake Request'` | Yes (K-06-class — **Still Present**, partially fixed) |
| NITRO-007 | scrITEstimate | line 227 | — | Functionality | ~~`'IntakeID Request'` column-name mismatch~~ | **RESOLVED** — corrected to `'Project Intake Request'` | No |
| NITRO-010 | scrHome | `drpQuickRole` | — | Role Access | ~~Role-switcher dropdown present on production Home screen~~ | **RESOLVED** — control removed entirely | No |
| NITRO-011 | scrHome | `btnRecentOverlay.OnSelect` | — | Functionality | ~~`Navigate()` fired without first re-fetching the record~~ | **RESOLVED** — now does `Set(varProjectRec, LookUp('Project Intake Requests', nod_projectintakerequestid = ThisItem.nod_projectintakerequestid))` before `Navigate()` | No |
| NITRO-012 | scrPMOQueue | `cmpPrimaryNav.Fill` | — | Design System | ~~Hardcoded `RGBA(1, 81, 52, 1)`~~ | **RESOLVED** — now `gblTheme.ColorBrandAccent` | Yes (K-11-class — **Resolved** at this location) |
| NITRO-013 | scrPMOQueue | `cmpHeader.EnvironmentLabel` / `cmpPrimaryNav.CurrentScreen` | — | ALM | ~~Placeholder literal `"Text"`~~ | **RESOLVED** — `EnvironmentLabel` now bound to `varEnvironmentLabel`; `CurrentScreen` now `"queue"` | No |
| NITRO-014 | scrPMOQueue | `btnReturnToSubmitter.OnSelect` | Medium | YAML Syntax | Formula is still written as a single inline-quoted string with escaped `\r\n` instead of the `\|-` block scalar style used everywhere else; this same string also still embeds the `'IntakeID Request'` wrong-column bug (third occurrence of NITRO-006's defect) | Convert to `\|-` block style; fix the embedded `'IntakeID Request'` → `'Project Intake Request'` reference inside it | No |
| NITRO-015 | scrPMOQueue | OnVisible | — | Role Access | ~~No real role-check gate before unconditionally clearing `varRedirectToHome`~~ | **RESOLVED** — nested `If(Not(varAppReady), true, If(Not(varIsAdmin Or varIsPMO Or varIsITEstimateManager), Set(varRedirectToHome,true), ...))` gate added (paren-balance verified); cosmetically messy indentation but functionally correct | No |
| NITRO-017 | scrITEstimate | `galEstimateRequests.OnSelect` | — | Functionality | ~~`ThisItem` hydration instead of live `LookUp()`~~ | **RESOLVED** — now `With({_rec: LookUp('Project Intake Requests', nod_projectintakerequestid = ThisItem.nod_projectintakerequestid)}, Set(varProjectRec, _rec); ...)` | Yes (K-03/K-05-class — **Resolved** at this location) |
| NITRO-031 | scrITEstimate | 3 modal-scrim `Fill` properties (≈lines 1035, 1571, 2023) | High | Design System / Functionality | Semi-transparent black scrim literals (`RGBA(0,0,0,0.45)`, `0.35`, `0.5`) were replaced with **fully-opaque** `gblTheme.ColorBorderStrong` / `gblTheme.ColorSurfaceAlt` / `Color.DarkGray` — none of these tokens carry an alpha channel, so all three modal overlays (submitting-spinner, estimate-input, notes panel) will now render as a solid opaque color instead of a dimmed see-through backdrop | Add a dedicated semi-transparent scrim token to `gblTheme` (e.g. `ColorScrim: RGBA(15,23,42,0.5)`) and use that, rather than repurposing an opaque surface/border token | No |
| NITRO-008 | scrMyRequests | lines 434-435 | High | Performance / Functionality | `ThisItem` captured directly instead of live `LookUp()` by GUID | Re-`LookUp()` by GUID before acting | Yes (K-05 — Still Present here; resolved on scrPMOQueue/scrITEstimate) |
| NITRO-009 | scrMyRequests | line 555 | High | Functionality | `Switch` literal `"Steering Committee Project"` likely doesn't match the real Choice label | Replace with fully-qualified enum + `Text()` wrap | No |
| NITRO-019 | scrITEstimate | lines 208, 1336, 1357 | High | ALM | Hardcoded email addresses still baked into `Office365Outlook.SendEmailV2` calls — unchanged | Move to Environment Variables | No |
| NITRO-026 | scrTestHarness | (entire screen) | Critical | ALM / Missing Source File | `scrTestHarness.txt` still absent from the repository | Confirm intentional removal; document | Yes (K-08 — cannot verify) |
| NITRO-027 | screens/AllScreens | (entire file) | High | ALM | Combined export file still diverges from the per-screen `.txt` exports (and changed again in this update — 1469 lines touched) — strong evidence the tracked per-screen files are not the canonical source of truth | Determine canonical source; remove or document the duplicate | No |
| NITRO-032 | scrITEstimate | `lblEstimateStatus.BorderColor`/`.Color` (≈lines 386, 408) | Medium | Design System | Two of the fifteen original hardcoded-RGBA spots were "fixed" by swapping in raw system color constants (`Color.Orange`, `Color.DarkOrange`, `Color.LightGoldenRodYellow`) instead of `gblTheme` tokens — still not a design-token reference, just a different hardcoding syntax | Map to the nearest `gblTheme` token (e.g. `gblTheme.ColorWarning`) | Yes (K-11-class — **New Variant**, not actually resolved) |
| NITRO-034 | scrITEstimate | `lblEstimateStatus.BorderColor`/`.BorderThickness` (≈lines 386, 395) | Medium | Functionality | Still compares `Text(ThisItem.'IT Estimate Status') = "In Review"` via string literal, while the adjacent `Color`/`Fill` properties on the *same control* were converted to fully-qualified enum members — inconsistent pattern within one control, and the literal-mismatch risk (NITRO-023) is only half-closed | Convert to `ThisItem.'IT Estimate Status' = 'IT Estimate Status (Project Intake Requests)'.'In Review'` to match the sibling properties | Yes (K-11/NITRO-023-class — **New Variant**) |
| NITRO-018 | scrITEstimate | lines 463, 1119, 1198, 1225, 1264, 2093, 2116 (7 of original 15) | Medium | Design System | Remaining hardcoded `RGBA(...)` literals not yet converted to `gblTheme` tokens (8 of the original 15 were fixed) | Map each to the nearest `gblTheme` token | Yes (K-11 — Still Present, partially resolved) |
| NITRO-020 | scrITEstimate | line 218/1302 vs line 1350 | Medium | Functionality | `ChangedBy` inconsistency persists — now `varAppUser.Name` in two audit `Collect()`s and `varCurrentSystemUserId` in the third (the majority/minority flipped but the inconsistency itself is unchanged) | Standardize on one identity representation | No |
| NITRO-022 | scrITEstimate | lines 290, 587, 647, 707, 766, 902, 937 | Medium | Naming | Default control names unchanged: `Rectangle3/6/7/7_2/9/10/11`, `DataGrid2` | Rename per `rec*`/`gal*`/`tbl*` convention | No |
| NITRO-025 | scrAdminQuestions | throughout | Medium | Design System | Screen still exclusively uses legacy non-`Modern*` controls | Migrate to Modern control set | No |
| NITRO-021 | scrITEstimate | line 1318 | Low | Design System | `BasePaletteColor: =gblColorText` still uses the legacy shim global | Replace with `gblTheme.ColorTextPrimary` | No |
| NITRO-024 | scrITEstimate | `btnCloseEstimateModal.OnSelect` | Low | YAML Syntax | Stray leading `=` blank line — unchanged | Remove the dangling blank line | No |
| NITRO-028 | App.OnStart | lines 121-125 | Low | ALM | Legacy shim globals retained alongside `gblTheme` — unchanged | Finish migration, then delete shim | No |
| NITRO-035 | scrPMOQueue | OnVisible | Low | YAML Syntax | The new role-gate `If()`/`Concurrent()` nesting added to fix NITRO-015 has inconsistent indentation (mixed 8/20-space levels) making the control flow hard to visually audit | Reformat with consistent indentation matching the rest of the file | No |
| Total | — | — | Critical: 3 | High: 6 | Medium: 7 | Low: 4 | — |

## Fixes Verified Since Last Review
**Fully resolved:** NITRO-001/K-02 (role flags), NITRO-002 (wizard resume-state overwrite), NITRO-003 & NITRO-004 (undefined `CategoryStepValid`), NITRO-005 (undefined `PMOStage`), NITRO-007 (scrITEstimate RemoveIf column), NITRO-010 (drpQuickRole removed), NITRO-011 (scrHome live LookUp before Navigate), NITRO-012 & NITRO-013 (scrPMOQueue placeholder/hardcoded nav properties), NITRO-015/K-10-adjacent (scrPMOQueue real role gate), NITRO-017/K-03/K-05-class (scrITEstimate gallery hydration).

**Partially resolved (downgraded, not closed):** NITRO-006 (1 of 3 `'IntakeID Request'` occurrences fixed — 2 remain, see NITRO-006 and NITRO-014), NITRO-018/K-11 (8 of 15 RGBA literals converted — 7 remain), NITRO-023 (Color/Fill switches now use real enum members, but introduced a typo — see NITRO-033 — and the sibling BorderColor/Thickness properties were left on string literals — see NITRO-034).

## New Regressions Introduced By This Edit Pass
- **NITRO-030 (Critical)** — malformed `Set(varSelectedApprovalRec, Blank();` in App.OnStart; a missing paren is masked by a compensating extra paren at the very end of the file, silently restructuring ~70 lines of sequential statements into one nested expression chain.
- **NITRO-033 (Critical)** — typo'd Choice enum member `'Information Needeed'` in scrITEstimate's status-color Switch; will likely fail to compile.
- **NITRO-031 (High)** — three modal scrims in scrITEstimate lost their transparency when "fixed" to use opaque `gblTheme`/`Color` tokens instead of semi-transparent RGBA.
- **NITRO-032 / NITRO-034 (Medium)** — two scrITEstimate "fixes" swapped one non-token hardcoding style for another, or only half-closed the underlying inconsistency.
- **NITRO-035 (Low)** — cosmetic indentation regression in scrPMOQueue's OnVisible.

**Net effect: this edit pass is a net improvement (10 fully-resolved findings vs. 5 new/regressed ones), but it is not safe to merge as-is** — NITRO-030 and NITRO-033 are both newly-introduced Critical defects that did not exist in the prior review and must be fixed before this branch can be considered closer to production-ready than before.
